query Input {
  cart {
    cost {
      totalAmount {
        amount
      }
    }
    buyerIdentity {
       customer {
       hasAnyTag(tags:["Veterinarian","Breeder"])
       }
    }
  }
  paymentMethods {
    id
    name
  }
}
